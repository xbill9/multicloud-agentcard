.venv/
.run/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
.coverage
.env
.aws_creds
*.log
infra/.verify-frozen.sh

# Stored card corpora. Regenerated by `agentcard fetch --save`.
# One directory per mesh -- see "A corpus holds one mesh" in README.md.
.cards/
.cards-deployed/

# A real peers file names deployed endpoints and account ids. Keep it local;
# peers.example.toml is the committed shape.
peers.toml
